# Settings file for numsim program
# Run ./numsim lid_driven_cavity.txt

# Problem description
physicalSizeX = 3   # physical size of the domain
physicalSizeY = 1
endTime = 30.0        # duration of the simulation
re = 200             # Reynolds number
gX = 0.0              # external forces, set to (gX,gY) = (0,-9.81) to account for gravity
gY = 0.0

# Dirichlet boundary conditions
dirichletBottomX = 0
dirichletBottomY = -0
dirichletTopX    = 0
dirichletTopY    = -0
dirichletLeftX   = 1 
dirichletLeftY   = 0
dirichletRightX  = 0
dirichletRightY  = 0

# Outflow boundary condintion  !!! Overrides dirichlet boundary condition if true
outflowTop = false
outflowBottom = false
outflowLeft = false
outflowRight = true

# Underrelaxation factors. Necessary for Outflow BC.
underrelaxationRHS = 1      # for calculating Right Hand Side. Best kept at 1
underrelaxationUV = 0.65     # for calculating u and v

# Discretization parameters
nCellsX = 2         # number of cells in x and y direction
nCellsY = 200
useDonorCell = true   # if donor cell discretization should be used, possible values: true false
alpha = 0.5           # factor for donor-cell scheme, 0 is equivalent to central differences
tau = 1             # safety factor for time step width
maximumDt = 0.5       # maximum values for time step width

# Solver parameters
pressureSolver = SOR  # which pressure solver to use, possible values: GaussSeidel SOR CG
omega = 1.6           # overrelaxation factor, only for SOR solver
epsilon = 1e-5        # tolerance for 2-norm of residual
maximumNumberOfIterations = 1e3    # maximum number of iterations in the solver
#maximumNumberOfIterations = 100    # maximum number of iterations in the solver

